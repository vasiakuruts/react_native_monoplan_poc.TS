import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles((theme) => ({
  wrapper: {
    display: 'flex',
    paddingLeft: 24,
    paddingRight: 24,
    marginTop: 10,
  }
}));
